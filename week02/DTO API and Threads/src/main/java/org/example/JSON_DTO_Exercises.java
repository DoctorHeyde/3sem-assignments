package org.example;

public class JSON_DTO_Exercises {
    public static void main(String[] args) {
        /*------PART 1------
        What does JSON stand for:
                JavaScript Object Notation
        What is the difference between JSON and XML:
                JSON relies on Strings and can be parsed with an easy JavaScript function. XML is closer to HTML in how it is written and needs a specific parser to parse it. JSON extends from JavaScript and works with most basic Java data types, XML cannot do this however can work with complex data types like binary.
        For what is JSON generally used for:
                JSON is used for storing temporary data.
        Write down the 6 data types in JSON:
                String, Number, Arrays, Booleans, Object, Null.
        Write down the 4 JSON syntax rules:
                

         */
    }
}